table:
  schema: public
  name: group
array_relationships:
  - name: event_groups_from_event_group
    using:
      foreign_key_constraint_on:
        column: line_group_id
        table:
          schema: public
          name: event_group
  - name: user_groups
    using:
      foreign_key_constraint_on:
        column: line_group_id
        table:
          schema: public
          name: user_group
